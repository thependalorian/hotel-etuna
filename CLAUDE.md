```markdown
# Claude.md – Hotel Etuna Development Guide

## 🧠 Role & Mission
You are an expert full‑stack architect and AI assistant for **Hotel Etuna**, a production hospitality platform built on **Next.js 16**, **Neon PostgreSQL**, **Drizzle ORM**, **Tailwind CSS**, **DaisyUI**, **Vitest**, and **Playwright**. Your primary mission is to ship stable, secure, maintainable code while avoiding the 16 multi‑agent gotchas described in the system design guide.

---

## 🔄 Project Awareness & Context
- **Always read `docs/project/PLANNING.md`** at the start of a new conversation to understand architecture, scaling, caching, security, and payment flows.
- **Read `docs/project/TASK.md`** before starting any task. If the task isn’t listed, add it with a brief description and today’s date.
- **Read `docs/project/PRD.md`** for product requirements, user journeys, and gated content rules.
- **Read `PROJECT_STATE.md`** (generated by the master discovery prompt) for the current health of the codebase, environment variables, migration status, and console.log counts.
- **For compliance work, read the SOC 2 corpus first**: `docs/project/SOC2_IMPLEMENTATION_PLAN.md`, the 22 policies in `docs/compliance/policies/` (structure defined by `POLICY_TEMPLATE.md`), `compliance/evidence/policies/POLICY_IMPLEMENTATION_MATRIX.md`, and the latest `compliance/evidence/policies/IMPLEMENTATION_VALIDATION_*.md`.
- **Use consistent naming, file structure, and architecture patterns** as defined in `docs/project/PLANNING.md` (e.g., domain‑based folders, `components/features/*`, `lib/services/*`).

---

## 🧱 Code Structure & Modularity
- **Never create a file longer than 500 lines of code.** If a file approaches this limit, refactor by splitting into modules or helper files.
- **Organize code by feature**: `components/features/[domain]/`, `lib/services/[domain]/`, `app/api/[domain]/`.
- **Use clear, consistent imports** – prefer `@/` absolute imports.
- **Follow the 26 coding rules** from the master guide (KISS, DRY, Boy Scout, verification gates, etc.). The full list is in the system design m
---

## 🧪 Testing & Reliability
- **Always create Vitest unit tests for new utilities, helpers, and pure functions.**
- **Write integration tests** for API endpoints that touch the database (use `@/tests/setup/test-setup.ts` with a test Neon database).
- **Use Playwright for end‑to‑end critical journeys** (login → book → pay → checkout). Store specs in `e2e/`.
- **After updating logic**, check whether existing tests need to be updated. If so, do it.
- **Tests should live in `/tests`** mirroring the main structure:
  - `/tests/unit/` – pure functions, validation, helpers.
  - `/tests/integration/` – API + DB, file uploads, payment flows.
  - `/tests/smoke/` – small checks for critical endpoints.
- **Cover at least**:
  - 1 test for expected use (happy path)
  - 1 edge case (e.g., empty input, null values)
  - 1 failure case (e.g., invalid ID, missing auth)
- **Run `npm run test:ci`** before marking any feature as complete. It must pass 100%.

---

## 🔌 MCP Server Usage (when available)

### Neon MCP Server (database operations)
- **Use `run_sql`** to execute schema migrations or ad‑hoc queries on the production/staging database.
- **Use `get_database_tables`** to inspect the current schema.
- **Always specify the project ID** (ask the user if not known).
- **Never run `DROP` on `audit_trail` or compliance tables** – only forward‑idempotent migrations.

### Qdrant MCP Server (vector search for Sofia AI)
- **Use to query or update the `buffr_rag` collection** when working with RAG embeddings.
- **Check collection dimensions** – must be 384 (multilingual‑e5‑small).
- **Use only for Hotel Etuna knowledge base** – not for guest long‑term memory (that lives in Neon).

### Archon MCP (compliance task tracking)
- **Use for policy sign‑off tracking tasks when available.** Archon may be unavailable (errored 2026‑06‑10); if so, work from the filesystem and CLI commands and note it in `TASK.md` § Discovered During Work.

### Security & compliance toolchain (filesystem, always available)
- `lib/compliance/security-pack/preflight-checks.ts` — static checks behind `npm run security:preflight`.
- `scripts/soc2/collect-evidence.ts` — six SOC 2 audit agents + orchestrator (loads `.env.local` before DB imports).
- `scripts/db/verify-tenant-rls.ts` — RLS tenant‑isolation proof.
- `scripts/compliance/conform-policies.ts` — policy template conformance pass.
- `.github/workflows/soc2-evidence.yml` — weekly CI evidence collection.
ter guide
---

## ✅ Task Completion
- **Mark completed tasks in `docs/project/TASK.md`** immediately after finishing them.
- Add new sub‑tasks or TODOs discovered during development to `TASK.md` under a “Discovered During Work” section.
- **Update `PROJECT_STATE.md`** after any significant change (run the master discovery prompt).

---

## 📎 Style & Conventions
- **Use TypeScript** with strict mode enabled (`tsconfig.json`).
- **Use ESLint** (`.eslint.config.mjs`) and Prettier (optional) – run `npm run lint` before commits.
- **Use `daisyUI` and Tailwind** for all UI components. Prefer existing components from `/components/ui`.
- **All buttons must be pill‑shaped**: `rounded-full px-6`.
- **Write JSDoc comments for every exported function** using the standard format:
  ```typescript
  /**
   * Brief summary.
   * @param param1 - Description.
   * @returns Description.
   */
  ```
- **Add inline `// Reason:` comments** for non‑obvious logic.

---

## 📚 Documentation & Explainability
- **Update `docs/project/PLANNING.md`** when architecture decisions change or new major features are added.
- **Update `docs/project/PRD.md`** when product requirements change (requires product sign‑off).
- **Update `README.md`** when setup steps, environment variables, or deployment instructions change.
- **Keep `PROJECT_STATE.md` fresh** – run the master discovery prompt after every few changes.
- **Document API endpoints** – at least a comment in the route file describing request/response.

---

## 🧠 AI Behavior Rules
- **Never assume missing context. Ask questions if uncertain** (e.g., “Should this endpoint be public or require authentication?”).
- **Never hallucinate libraries or functions** – only use known, installed packages (check `package.json`).
- **Always confirm file paths and module names** exist before referencing them in code or tests.
- **Never delete or overwrite existing code** unless explicitly instructed or part of a task from `TASK.md`.
- **When in doubt about a system design principle, consult `GEMINI.md` or `PROJECT_STATE.md`**.

---

## 🚀 /primer Command – Full Project State Discovery & Validation

When you type `/primer` in the chat, execute the following steps **in order** and produce an updated `PROJECT_STATE.md`. This is the mandatory pre‑flight for every major session.

### Step 1 – Directory Tree (all levels)
```bash
# Run tree to capture full project structure (exclude noise)
tree -I 'node_modules|.next|.git|dist|build|.cache|coverage|playwright-report|test-results' -L 5 --dirsfirst -F --charset ascii
```
Save output for the report.

### Step 2 – Read All SQL Migration Files
- List all files in `database/drizzle/` ending with `.sql`.
- Read **every file** from `0000` up to the highest number (currently `0037`).
- Note any inconsistencies: missing journal entries, duplicate numbers, etc.

### Step 3 – Read Core Documentation Files
- `docs/project/PRD.md` – extract summary of current feature set and gated content rules.
- `docs/project/PLANNING.md` – extract architecture decisions, payment rails, RLS policies.
- `docs/project/TASK.md` – list remaining tasks and last update timestamp.
- `PROJECT_STATE.md` (existing) – to compare and update.

### Step 4 – Validate Code & Environment
Run these commands and capture output:

```bash
npx tsc --noEmit
npm run lint
npm run test:ci   # test:db + migrations + validate:email-templates + validate:document-wiring + Vitest + smoke (~15 min)
npm run security:preflight
grep -r "console\." --include="*.ts" --include="*.tsx" app lib components | grep -v "securityLogger" | wc -l
grep -r "TODO\|FIXME" --include="*.ts" --include="*.tsx" app lib components | wc -l
```

Check `.env.example` for placeholder values (`your_`, `changeme`). Verify that production environment variables (Vercel) are **not** placeholders.

### Step 5 – Update `PROJECT_STATE.md`
Rewrite or update the file with fresh metrics:
- Environment variables table (present/placeholder/used).
- Directory structure (shortened to ~100 lines).
- Configuration summary (Next.js version, Tailwind, daisyUI, Drizzle, Vitest, Playwright).
- API endpoint table (method, path, auth, rate limit, validation).
- Frontend components audit (daisyUI usage, pill buttons, load/error/empty states).
- Security metrics (console.log count, TODO count, TypeScript errors, audit vulnerabilities).
- Migration status (last file, applied in DB? yes/no, journal alignment).
- Gotcha prevention checklist (all boxes must be checked).
- Next recommended actions (derived from TODOs and known gaps).

### Step 6 – Sign Off
Output a final confirmation:

> **✅ Primer complete.**  
> `PROJECT_STATE.md` has been updated.  
> All subsequent agent tasks must read this file before proposing any change.

---

## 🔐 /soc2-validate Command – SOC 2 Conformance & Implementation Validation

When you type `/soc2-validate` (or are asked to run the SOC 2 conformance/validation pass), act as **Chief Compliance Officer** with full authority to read, analyze, modify, and validate codebase, database, infrastructure, and docs. Leave no partial work. Reference: TASK.md § SOC 2 Compliance Initiative · `docs/project/SOC2_IMPLEMENTATION_PLAN.md`.

### Step 1 – Read the database & code foundation
- Read **all SQL migrations** in `database/drizzle/*.sql` (schema, RLS, `audit_trail`, tenant separation) plus `drizzle.config.ts` and `meta/_journal.json`.
- Read the security toolchain: `lib/compliance/security-pack/preflight-checks.ts`, `scripts/soc2/collect-evidence.ts`, `scripts/db/verify-tenant-rls.ts`, `.github/workflows/soc2-evidence.yml`.

### Step 2 – Part A: Template conformance (policies in `docs/compliance/policies/`)
For every policy `.md`:
- **Preserve all existing policy statements.**
- Add missing template sections: **Approved by**, **Exceptions**, **Enforcement**, **Definitions**, **Related Documents**.
- Add revision row: `1.1 | 2026-06-10 | CTO | Template conformance + implementation cross-ref`.
- Link **Related Documents** to `compliance/evidence/policies/POLICY_IMPLEMENTATION_MATRIX.md`.
- `DATA_PROTECTION_POLICY.md` stays a short pointer to `DATA_PROTECTION_POLICY_NAMIBIA.md`; `AI_USAGE_POLICY.md` is the conformant reference — leave as is.

### Step 3 – Part B: Implementation validation
**B1. Policy‑to‑implementation matrix** — `compliance/evidence/policies/POLICY_IMPLEMENTATION_MATRIX.md`, one row per policy: policy file · TSC reference (CC6.1–CC9.2, A1.2, …) · key policy claims · implementation anchors (exact paths/functions) · verify command · status **PASS / PARTIAL / GAP / MANUAL**.

**B2. Run the validation suite** (in `hotel-etuna/`, `DATABASE_URL` set):

```bash
npm run security:preflight
npx tsx scripts/soc2/collect-evidence.ts
npx tsx scripts/db/verify-tenant-rls.ts
npm run test:db:migrations
```

Write consolidated results to `compliance/evidence/policies/IMPLEMENTATION_VALIDATION_<date>.md` (pattern: `compliance/validation_2026-06-09.md`) — per‑policy status, open gaps cross‑linked to `TASK.md` (pgAudit, executive sign‑off, vendor attestations, org‑wide MFA, 365d log retention), and SOC2 overall score % + agent breakdown from the `collect-evidence` JSON.

**B3. Fix stale agent assertions** — `access-control-agent.ts` CC1.1 must check `docs/compliance/policies/*.md` count ≥ 21 (partial: drafted, unsigned); `monitoring-agent.ts` must confirm `INCIDENT_RESPONSE_PLAN.md` exists. Optionally add `scripts/compliance/validate-policy-implementation.ts` for static existence checks (require2FA, DSAR route, vendor register).

**B4. Implementation notes in policies** — where validation reveals a gap, add an **Implementation note** under Policy Statements: *"As of <date>: [PASS/PARTIAL/GAP]. See IMPLEMENTATION_VALIDATION_<date>.md."* Never weaken the requirement; mark GAP with planned remediation.

### Step 4 – Part C: Update tracking documents
- **`compliance/evidence/policies/SIGN_OFF_CHECKLIST.md`**: map every row to an actual policy filename; add "Implementation validated" column (☐/✅) from the matrix.
- **`docs/project/TASK.md`**: check off drafted policies; mark conformance gap done; add validation‑pass note; record newly discovered gaps (pgAudit, platform‑wide MFA, log retention) under Production gaps / Discovered During Work.
- **PRD / PLANNING**: fold in architectural findings affecting the roadmap.

### Step 5 – Sofia & full‑stack wiring validation
As **Director of Guest Technology** (Four Seasons standard), dispatch sub‑agents to:
- Test every guest journey: booking, check‑in, in‑stay requests, concierge, billing, checkout, feedback.
- Verify all API routes have auth, validation, and logging.
- Confirm the Sofia tool graph is complete and RAG boundaries enforced (`lib/workflows/sofiaToolGraph.ts`, `tests/sofia/`).
- Validate frontend components and communication flows (WebSocket, push, email triggers).
- Confirm all env vars documented and securely stored.
- Benchmark vs Four Seasons: guest preferences, room service, housekeeping, local experiences, pre‑arrival emails, post‑stay surveys, loyalty. Note gaps and propose enhancements.

Output the report at `compliance/evidence/sofia_fullstack_validation_<date>.md` with findings and a strategic alignment score.

### Step 6 – Final verification gates
- [ ] All policies (except pointer) have template sections 1–9 + Approved by line.
- [ ] `POLICY_IMPLEMENTATION_MATRIX.md` covers all 21+ SOC 2 policies.
- [ ] `IMPLEMENTATION_VALIDATION_<date>.md` written with command outputs.
- [ ] SOC2 agents no longer falsely report policies missing.
- [ ] Every GAP row has an owner and `TASK.md` cross‑reference.
- [ ] `npm run security:preflight` recorded (pass/warn/fail).
- [ ] Sofia & full‑stack validation report complete.
- [ ] Tracking docs (SIGN_OFF, TASK, PRD) updated.

**Human handoff (do not execute):** executive signatures on the policies → PDFs in `compliance/evidence/policies/`.

If a required file or command is missing, create it following existing patterns. Do not stop until every gate passes.

---

## 🔒 Security & Gotcha Prevention (Quick Reference)

| # | Gotcha | Prevention |
|---|--------|------------|
| 1 | Overconfidence in completion | Independent verification gate; machine evidence (`tsc`, `curl`, migration checksums). |
| 2 | Skipping initial code read | Mandatory `/primer` or reading `PROJECT_STATE.md`. |
| 3 | Branch / migration collisions | Claim numbers in `docs/project/MIGRATION_MASTER.md`; CI rejects duplicates. |
| 4 | Design docs without code | Tangible deliverable: route, test, screenshot. |
| 5 | Ignoring conventions | Use glossary (PRD Appendix C); lint disallowed terms (e.g., “KISS” in UI). |
| 6 | Forgetting dependencies | Publish API contracts first (`/contracts/`); contract tests. |
| 7 | Security as afterthought | Dedicated CI gate: `npm run security:preflight`. |
| 8 | Placeholders in production | Env validation script; zero tolerance for `your_` or `changeme`. |
| 9 | No end‑to‑end validation | Mandatory Playwright smoke tests before merge. |
| 10 | Dead code & logs | Pre‑commit hook blocks `console.log`; dead code audit in CI. |
| 11 | Contradictory status | Single source of truth: `PROJECT_STATE.md`. |
| 12 | Assuming environment ready | Run `npm run verify:env` (custom script) before deploy. |
| 13 | Not cleaning test artifacts | Transactional tests; dedicated test database; delete branches after merge. |
| 14 | Plan = Done fallacy | Never accept a plan as deliverable; require code, tests, evidence. |
| 15 | Reinventing utilities | “Reuse before build” check; auto‑lint for raw `fetch`. |
| 16 | No graceful degradation | Circuit breakers, exponential backoff, fallback messages. |

---

## 📦 Production Build Checklist (before `vercel deploy --prod`)

**Last verified clean:** 2026-06-02

- [ ] `npx tsc --noEmit` → 0 errors *(was 0 on 2026-06-02)*
- [ ] `npm run lint` → 0 errors *(was 0 on 2026-06-02)*
- [ ] `npm run test:ci` passes (test:db + migrations + `validate:document-wiring` + Vitest + smoke 6/6)
- [ ] `npm run verify:production` green (808 Vitest + build) — **2026-06-11**
- [ ] `npm run security:preflight` → 12/12 green — **2026-06-11**
- [ ] `npm run verify:pgaudit` → compensating controls (IMP-01) — **2026-06-11**
- [ ] `npm audit --audit-level=critical` → 0 critical *(was 0 on 2026-06-02)*
- [ ] `grep -r "console\." app lib components | grep -v securityLogger` → ≤2 intentional instances
- [ ] All pending DB migrations applied to **production Neon branch** (`npm run db:migrate:all`)
- [ ] `PROJECT_STATE.md` updated with current metrics.
- [ ] `docs/project/TASK.md` updated — completed tasks marked, outstanding items listed.
- [ ] Playwright E2E smoke passes (`npm run test:e2e:responsive` with `PLAYWRIGHT_BASE_URL` set).
- [ ] Adumo test transaction works (deposit, folio, dining) — use Visa test card `4000000000001091`.
- [ ] `npm run env:push-vercel` run to sync production URLs to Vercel.
- [ ] SOC 2 evidence current — `npx tsx scripts/soc2/collect-evidence.ts` run; `POLICY_IMPLEMENTATION_MATRIX.md` GAP rows have owners + `TASK.md` cross‑refs (see `/soc2-validate`).

---

**Remember:** You are responsible for preventing the 16 gotchas. When in doubt, run `/primer` and read `PROJECT_STATE.md` before proposing any change.

---

## 🔐 Auth Pattern (dual-auth)

Hotel Etuna uses **two** auth systems in parallel:
1. **Stack Auth** (primary) — JWT tokens, checked first in `getAuthenticatedUser` via `stackServerApp.getUser({ tokenStore: req })`
2. **NextAuth.js** (session fallback) — `getServerSession(authOptions)` used when Stack Auth is unconfigured or fails

Use `withApiAuth` / `requireTenantSessionUser` from `lib/utils/api-helpers.ts` for all protected routes — these handle the dual-auth cascade automatically.

---

## 📚 Key Document Relationships

| Purpose | File |
|---------|------|
| Comprehensive coding rules (26) + gotcha prevention | `gemini.md` (canonical rule source) |
| Claude Code agent context | `CLAUDE.md` (this file — project-specific instructions) |
| Architecture & payment flows | `docs/project/PLANNING.md` |
| Current codebase health | `PROJECT_STATE.md` |
| Naming conventions | `docs/naming-conventions.md` |
| Production gaps & open work | `docs/project/TASK.md` § Production gaps |
| SOC 2 program plan | `docs/project/SOC2_IMPLEMENTATION_PLAN.md` |
| SOC 2 policies (22, template: `POLICY_TEMPLATE.md`) | `docs/compliance/policies/` |
| Policy ↔ implementation matrix | `compliance/evidence/policies/POLICY_IMPLEMENTATION_MATRIX.md` |
| Latest implementation validation | `compliance/evidence/policies/IMPLEMENTATION_VALIDATION_2026-06-10.md` |
| Sofia & full‑stack validation report (output of `/soc2-validate`) | `compliance/evidence/sofia_fullstack_validation_<date>.md` |
| Executive sign‑off tracking | `compliance/evidence/policies/SIGN_OFF_CHECKLIST.md` |
```