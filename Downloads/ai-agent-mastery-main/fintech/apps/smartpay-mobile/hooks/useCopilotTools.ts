/**
 * useCopilotTools – Native (iOS/Android). No-op placeholder.
 * Note: We do NOT use CopilotKit web integration. CopilotKit web files have been removed.
 * CopilotKit pulls in @segment/analytics-node which uses Node's "node:buffer" and breaks React Native.
 * Tools are available via services/copilotTools; use Copilot backend or UI-triggered flows.
 * Location: fintech/smartpay/hooks/useCopilotTools.ts
 */
export function useCopilotTools() {
  // No-op on native. Tools are available via services/copilotTools; use Copilot backend or UI-triggered flows.
}
